# Server
NODE_ENV=development
PORT=3000

# SQLite file path (use ":memory:" for an ephemeral in-memory database)
SQLITE_FILE=./data/app.db

# CORS (comma separated origins)
CORS_ORIGIN=http://localhost:5173
