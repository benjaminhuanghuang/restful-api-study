# Restful API + TS Setup

```sh
npm init - y


npm install express cors body-parser

npm install -D typescript @types/node @types/express @types/cors

npx tsc --init

```

Script

```json
"dev": "node --watch server.ts"
```
